#include<bits/stdc++.h>
 using namespace std;
  int main() {
     // stack 
      // it holds the lifo property there is only three  which is the top() , size(), push(),pop()
            stack<int> st;
             st.top(); // it will tell that who is at the top of the stack;
              st.size(); // it will tell the what is the size of the stack;
               st.pop(); // it will pop the last element from the stack;
                st.push(); // it will push the  element in the stack at the last; 
     return 0;
  }