#include<bits/stdc++.h>
 using namespace std;
  
  int main() {
        // list
        //  list is similar to the vector but the basic difference is that 
        //   you can perform operations in the list from the front as well
        //    v.push-front() --> you can use this functions to add the element in the front of the
        //     list there is insert function in the vector but using the insert is costlier than the 
        //           using the push-front() thats why you can use it 
      // rest functions are similar to the vector in the list
    return 0;
  }